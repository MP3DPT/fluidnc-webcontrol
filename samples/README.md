# Sample G-code

`gcode_test.gcode` - a small, safe test cut (a shallow pocketed ring, ~47×31×5mm, well within any machine's travel) used throughout this project's own testing. Handy for a first Run after setup, or for verifying a plugin (smart plug, notifications) fires correctly without committing to a real job.
