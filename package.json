{
  "name": "fluidnc-webcontrol",
  "private": true,
  "version": "0.2.0",
  "description": "Free, open-source, community-driven web control UI for FluidNC CNC controllers.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "build:backend": "npm run build -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build:frontend": "npm run build -w frontend",
    "start": "npm run build:frontend && npm run build:backend && npm run start -w backend",
    "sync-plugins": "node scripts/sync-plugins.mjs",
    "sync-plugins:check": "node scripts/sync-plugins.mjs --check"
  },
  "license": "MIT"
}
